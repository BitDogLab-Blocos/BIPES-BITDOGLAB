# GitHub Pages deployment intentionally disabled while experimental work is in progress.
name: GitHub Pages deploy disabled

on:
  # Manual-only placeholder. Pushes to main will not deploy.
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment disabled
        run: echo "GitHub Pages deploy is disabled for experimental work."
